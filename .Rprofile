## default repo
local({r <- getOption("repos")
  r["CRAN"] <- "http://cran.r-project.org"
  options(repos=r)
})

options(editor="vim")
options(prompt="➤ ")

q <- function (save="no", ...) {
  quit(save=save, ...)
}
